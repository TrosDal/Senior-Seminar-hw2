#!/usr/bin/python
"""Write here."""

def write_primes(num_list, file_name):
    """Takes a list of prime numbers and writes them to a csv file."""
    print(7)

def read_primes(file_name):
    """Reads prime numbers from a csv file and places them into a list, which it returns."""
    print(9)
